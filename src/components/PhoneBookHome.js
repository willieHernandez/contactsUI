import React, {Component} from 'react';


class PhoneBookHome extends Component{

    render(){
        return(
            <div>
                <h1>Contacts Application Home Page</h1>
                {/* Create a nav bar that gives the user the ability to view phone book, add a person, edit the phonebook, delete a person*/}

                {/* Give a search bar to allow them to search for a contact from the home page. */}

                {/* Give an overview of the phone book and cover
                    how many contacts they have
                    contacts by type */}

            </div>
        )
    }
}


export default PhoneBookHome;